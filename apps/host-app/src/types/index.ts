/**
 * Type re-exports from shared types package
 * Provides convenient access to all shared types
 */

export type {
  GameState,
  GamePhase,
  GameResults,
} from '@allstars/types';

export type {
  Question,
  QuestionChoice,
  GamePeriod,
  QuestionType,
} from '@allstars/types';

export type {
  HostAction,
  HostActionRequest,
  HostActionResponse,
  HostUser,
} from '@allstars/types';
