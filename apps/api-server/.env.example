# Application Configuration
# Note: Firebase emulator environment variables (FIRESTORE_EMULATOR_HOST, etc.)
# are automatically set by the Firebase emulator and should NOT be in .env
NODE_ENV=development
LOG_LEVEL=debug

# Firebase Service Account (for custom token generation)
# Path to service account JSON file - excluded from version control
# For development: Use relative path to service-account-dev.json in repo root
# For production: Use absolute path to secure service account file
GOOGLE_APPLICATION_CREDENTIALS=../../service-account-dev.json

# Projector Authentication
# Static API key for /api/projector/auth-token endpoint (minimum 32 characters)
# Generate with: openssl rand -hex 32
# Same key must be in projector-app VITE_PROJECTOR_API_KEY
PROJECTOR_API_KEY=your-secure-api-key-here-min-32-chars
